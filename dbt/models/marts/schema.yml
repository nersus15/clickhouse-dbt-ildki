# Folder marts/ SUDAH TIDAK DIPAKAI -- semua model dipindah ke models/staging/fhir/
# (lihat readme.md, bagian "Struktur Proyek"). File ini sengaja dibiarkan minimal/kosong
# (bukan dihapus, tool yang dipakai tidak punya kapabilitas delete file) supaya tidak
# ikut ke-parse dbt sebagai properties file yang mengacu ke model yang sudah tidak ada.
version: 2
